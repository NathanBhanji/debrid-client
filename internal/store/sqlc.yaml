version: "2"
sql:
  - engine: "sqlite"
    schema: "migrations"
    queries: "queries"
    gen:
      go:
        package: "sqlcgen"
        out: "sqlcgen"
        emit_interface: true
        emit_empty_slices: true
        emit_result_struct_pointers: false
